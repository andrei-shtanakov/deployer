# Pinned real-world targets for `deployer bench run --include-external`.
# No entries yet: candidates (locallogai) enter after Phase 4 system-deps
# hardening. Format:
#
# [[targets]]
# name = "some-project"
# url = "https://github.com/owner/repo.git"
# commit = "<full sha>"
# [targets.target]        # optional DeployTarget fields
# [targets.expected]      # optional ExpectedOutcome fields
# expected_success = false

# Local research target (machine-specific path; private EPAM GitLab remote).
# Build-only intent: the real app (app.py, Gradio) needs the 'gui' extra,
# which deploy_target cannot express yet — recorded as a Phase 4b need.
[[targets]]
name = "locallogai-backend"
url = "/Users/Andrei_Shtanakov/lab_aist/locallogai-backend"
commit = "a8309d71b93598a402f7c1f0cd92af8fa2626667"
[targets.expected]
expected_success = true
max_iterations = 3
requires_l2 = true
capabilities = ["uv", "external", "build-only", "python-3.13"]
notes = "uv + psycopg[binary], py>=3.13; main.py is a stub (build-only target); gradio app needs extras support (Phase 4b)"
